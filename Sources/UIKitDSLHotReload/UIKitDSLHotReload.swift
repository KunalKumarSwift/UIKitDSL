import Foundation
import UIKitDSLCore
import UIKitDSLArchitecture

// UIKitDSLHotReload — placeholder for debug-only hot reload infrastructure.
// This entire module is compiled only in DEBUG builds via the DEBUG Swift define.
// Future home of FileWatcher, LiveReloadServer, HotReloadController, etc.

#if DEBUG
// Hot reload support is active in debug builds only.
#endif
